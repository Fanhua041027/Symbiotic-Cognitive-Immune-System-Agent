# Core dependencies
langgraph>=0.4.0
langchain-openai>=0.3.0
python-dotenv>=1.1.0

# Webhook / notification support (v1.2.0)
requests>=2.31.0

# Optional: persistent immune memory (falls back to in-memory)
chromadb>=0.6.0

# Optional: Web UI
streamlit>=1.30.0

# Optional: REST API
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0

# Optional: ONNX local embeddings
onnxruntime>=1.17.0
numpy>=2.2.6
tokenizers>=0.15.0

# Optional: development
pytest>=8.0
pre-commit>=3.0
ruff>=0.4
